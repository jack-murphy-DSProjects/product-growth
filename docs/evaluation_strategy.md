# Evaluation Strategy

Evaluation is not implemented in Package -1.

Future evaluation work should compare model behavior against credible commercial
baselines and operating metrics, not only generic classifier metrics. Evaluation
outputs are generated artefacts and must remain out of git unless explicitly
added as safe examples in a later package.
